{
  "database": {
    "rules": "database.rules.json"
  },
  "hosting": {
    "public": "client-front",

    "rewrites": [
      {
        "source": "**",
        "destination": "/public/index.html"
      }
    ]
  },
  "storage": {
    "rules": "storage.rules"
  }
}
