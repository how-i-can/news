const calculateHours = require("./calculateHours");

describe("calculateHours", () => {
  it("exists", () => {
    // Run the assertion here, the test itself
    expect(typeof calculateHours).toBe("function");
  });
});
